import { styled } from '../stitches.config'

export const Wrapper = styled('div', {
  display: 'flex',
  flexDirection: 'column',
  minHeight: '100vh',
  position: 'relative',
  zIndex: 0,
  maxWidth: '1200px',
  marginLeft: 'auto',
  marginRight: 'auto',
})
